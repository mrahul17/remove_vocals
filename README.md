**remove_vocals** is a command line tool to remove vocals from music. It uses the **Pygraphics module**.


The algorithm is given by _Prof Daniel Zingaro_, University of Toranto

**Usage**:

python main.py _filepath_
